import { Modal, Button } from 'react-bootstrap';
export default function UnauthorizedUser(){
	const 
	return (
		<Modal>
			<h1>You are unauthorized to perform the previous action. Please login to get auth token</h1>
			<div>Go to login</div>
		</Modal>
		)
}